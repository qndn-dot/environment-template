mariadb:
  auth:
    database: bitnami_matomo
    username: bn_matomo
  tls:
    enabled: true
    autoGenerated:
      enabled: true
      engine: cert-manager
      certManager:
        existingIssuer: private-ca-cluster-issuer
        existingIssuerKind: ClusterIssuer
