mariadb:
  tls:
    enabled: true
    autoGenerated:
      enabled: true
      engine: cert-manager
      certManager:
        existingIssuer: private-ca-cluster-issuer
        existingIssuerKind: ClusterIssuer
  initdbScriptsConfigMap: ""