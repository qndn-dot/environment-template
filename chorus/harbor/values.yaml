harbor:
  expose:
    tls:
      certSource: secret
      secret:
        secretName: harbor.qa.chorus-tre.ch-tls
    ingress:
      hosts:
        core: harbor.qa.chorus-tre.ch

  externalURL: https://harbor.qa.chorus-tre.ch

  database:
    external:
      host: "chorus-qa-harbor-db-postgresql"

  redis:
    external:
      addr: "chorus-qa-harbor-cache-valkey-primary:6379"

certificate:
  enabled: true

  issuerRef:
    name: private-ca-cluster-issuer
    kind: ClusterIssuer
