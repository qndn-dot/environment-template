mariadb:
  auth:
    existingSecret: matomo-db-secret
    username: bn_matomo
    database: bitnami_matomo
  tls:
    enabled: true
    autoGenerated:
      enabled: true
      engine: cert-manager
      certManager:
        existingIssuer: selfsigned-issuer
        existingIssuerKind: ClusterIssuer
