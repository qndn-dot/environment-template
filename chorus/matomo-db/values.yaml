mariadb:
  fullnameOverride: chorus-qa-matomo-db
  auth:
    database: bitnami_matomo
    username: bn_matomo
  tls:
    enabled: true
    existingSecret: matomo-db-tls-secret
    autoGenerated:
      enabled: true
      engine: cert-manager
      certManager:
        existingIssuer: private-ca-cluster-issuer
        existingIssuerKind: ClusterIssuer
